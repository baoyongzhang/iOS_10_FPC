{ Parsed from Security.framework SecBase.h }

{$ifndef _SECURITY_SECBASE_H_}

{$ifdef TYPES}
type
  SecCertificateRef = OpaqueType;
  SecIdentityRef = OpaqueType;
  SecKeyRef = OpaqueType;
  SecPolicyRef = OpaqueType;
  SecAccessControlRef = OpaqueType;
{$endif}

{$endif}
